language: clojure
branches:
  except:
    - gh-pages
after_success:
  - lein codox
deploy:
  - provider: script
    cleanup: false
    script: lein deploy
    on:
      branch: master
  - provider: pages
    cleanup: false
    keep_history: true
    token: $GITHUB_TOKEN
    local_dir: codox
    on:
      branch: master
